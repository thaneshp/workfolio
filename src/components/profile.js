import React, { Component } from 'react';
import { withRouter } from 'react-router'

const Profile = () => {
    return(
        <div className="text-center"><h2>Profile Page</h2></div>
    )
}

export default withRouter(Profile);
